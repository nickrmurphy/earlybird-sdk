{
  "name": "earlybird-sdk",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.0",
    "@vitest/ui": "^3.2.0",
    "prettier-plugin-sort-json": "^4.1.1",
    "vitest": "^3.2.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter demo-client --filter demo-server dev",
    "test": "vitest",
    "test:ui": "vitest --ui --watch --coverage",
    "test:coverage": "vitest run --coverage"
  }
}
