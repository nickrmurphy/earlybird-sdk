{
  "name": "core",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun run typecheck && bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
