{
	"name": "@earlybird-sdk/storage",
	"version": "0.1.0",
	"description": "Storage abstractions and adapters for the Earlybird SDK",
	"module": "src/index.ts",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "bun run typecheck && bun test --coverage",
		"test:watch": "bun test --watch",
		"test:coverage": "bun test --coverage",
		"test:coverage:verbose": "bun test --coverage --reporter=verbose",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5",
		"@capacitor/filesystem": "^7.1.1"
	},
	"keywords": ["storage", "local-first", "capacitor", "filesystem", "adapters"]
}
